#include <stdio.h>
#include <conia.h>
void main (){
    int x,y
    int swap (int*,int*);
    ciscr();
    printf ("\nenter value of x:");
    scanf("%d",&x);
    swap(&x,&y);
    printf("\n\nvalues in the main():x=%d,y=%d",x,y);
    return 0;
}